1. Predicate<T> || boolean(T t) - проверяет удовлетворяет ли состояние
объекта условию.
2. Consumer<T> || void(T t) - выполняет действие без возврата результата
3. Function<T, R> || R(T t) - принимает в параметры один тип а возвращает
другой
4. Supplier<T> || T() - возвращает результат без передачи параметра
5. UnaryOperator<T> || T(T t) - принимает и возвращает один тип данных
6. BinaryOperator<T> || T(T t1, T t2)